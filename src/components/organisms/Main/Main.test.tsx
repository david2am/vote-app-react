import { Main } from './Main'
import { render, screen } from '@testing-library/react'

/* Test Cases

*/

describe('* Main tests', () => {
  it('Should be', () => {
    expect(true).toBe(true)
  })
})